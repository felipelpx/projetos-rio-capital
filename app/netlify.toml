[build]
  command = "npm run build"
  publish = "dist"

# Uma aplicação de página única: qualquer endereço devolve o index.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
